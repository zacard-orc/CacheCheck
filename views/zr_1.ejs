<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>ZRender</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <script src="/zr/js/jquery.js"></script>
    <script src="/zr/js/esl/esl.js"></script>
    <link href="/zr/css/bootstrap.css" rel="stylesheet">
    <link href="/zr/css/bootstrap-responsive.css" rel="stylesheet">

</head>
<body>
<div id="main" class="main"></div>
<script>
    var zr; // 全局可用zrender对象
    var domMain = document.getElementById('main');
    require.config({
        packages: [
            {
                name: 'zrender',
                location: './zrender/src',
                main: 'zrender'
            }
        ]
    });
    var color;
    var colorIdx = 0;
    var width;
    var height;
    require(
            ['zr', 'zr/src/shape/rose', 'zr/src/shape/trochoid'],
            function (zrender) {

                zr = zrender.init(domMain);
                color = require('zrender/tool/color');

                width = Math.ceil(zr.getWidth());
                height = Math.ceil(zr.getHeight());
                addjuxing();
            }
    )
</script>
</body>
</html>